{
  "name": "golfable",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cards:render": "npm run render --workspace=@golfable/card-generator",
    "web:dev": "npm run dev --workspace=@golfable/web-app"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.0",
    "stripe": "^22.5.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/web-push": "^3.6.4",
    "@vercel/node": "^3.2.0"
  }
}
